<UserControl x:Class="SparkValueDesktopApplication.Views.BreadboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SparkValueDesktopApplication.Views"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="850">
    <DockPanel>
        <Border DockPanel.Dock="Left" MinWidth="200" Background="LightSlateGray">
            <ScrollViewer>
                <StackPanel>
                    
                </StackPanel>
            </ScrollViewer>
        </Border>
        
        <StackPanel DockPanel.Dock="Right" Margin="5 0 0 0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid VerticalAlignment="Top" HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="Clear the Breadboard" Command=""/>
                <Button Grid.Column="2" Content="Return to Menu" Command=""/>
            </Grid>
            
            <Canvas Margin="20" Width="550" Height="200" Background="LightGoldenrodYellow">

            </Canvas>

            <Grid VerticalAlignment="Bottom" HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                
                <Border Grid.Column="0" Background="LightSlateGray">
                    <StackPanel>
                        <TextBlock HorizontalAlignment="Center" Text="Filters"/>
                        <CheckBox Margin="10 0 0 0" Padding="5" VerticalContentAlignment="Center" FontSize="12" Content="Components Only"/>
                        <CheckBox Margin="10 0 0 0" Padding="5" VerticalContentAlignment="Center" FontSize="12" Content="Component Values"/>
                    </StackPanel>
                </Border>
                
                <Border Grid.Column="2" Background="LightSlateGray" MinWidth="250">
                    <StackPanel>
                        <TextBlock HorizontalAlignment="Center" Text="Component Title"/>
                        <TextBlock Text="Component Description"/>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </DockPanel>
</UserControl>
